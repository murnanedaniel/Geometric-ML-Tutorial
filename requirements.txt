# === Geometric ML Tutorial — dependencies ===
# Tested on Python 3.10–3.11

# Core
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
tqdm>=4.65

# Notebook 03 (Feynman-diagram GNN): PyTorch + PyG + networkx
torch>=2.0
torch_geometric>=2.4
networkx>=3.0

# Notebook 02 (metallic glass GNN): materials informatics stack
# matminer pulls in pymatgen automatically
matminer>=0.9
pymatgen>=2024.1
# CHGNet provides the pretrained universal potential for Part B of notebook 02
chgnet>=0.4
# ase is required for structural relaxation (CHGNet's ASE calculator)
ase>=3.22

# Jupyter (only needed if running the notebooks locally; Colab has them preinstalled)
jupyter>=1.0
ipykernel>=6.25
