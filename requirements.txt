# === Geometric ML Tutorial — dependencies ===
# Tested on Python 3.11 with numpy 2.x, pymatgen 2026.x, matminer 0.10.x.

# Core
numpy
pandas
matplotlib
scikit-learn

# Both notebooks
torch>=2.0
torch_geometric>=2.4

# Notebook 02 (crystal structure GNN)
matminer>=0.9.3
pymatgen

# Notebook 03 (Feynman diagrams GNN) — additionally needs networkx
networkx>=3.0

# Jupyter (only needed if running locally)
jupyter>=1.0
ipykernel>=6.25
